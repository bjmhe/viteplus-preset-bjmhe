name: Unit Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions: {}

jobs:
  unit-test:
    uses: bjmhe/workflows/.github/workflows/unit-test.yml@feat/release-vp

  # coverage:
  #   uses: sxzz/workflows/.github/workflows/coverage.yml@main
  #   needs: unit-test
  #   permissions:
  #     id-token: write
